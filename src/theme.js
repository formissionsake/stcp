import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#eb1c8c'
        },
        secondary: {
            main: '#ff9445'
        },
        textPrimary: {
            main: '#ffffff'
        }
    }
});

export default theme;
